#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""生成 scenario_spoken_answers.py"""

from pathlib import Path

OUT = Path(__file__).parent / "scenario_spoken_answers.py"

RAW = r'''
1||用一个全局标志位记录 toast 是否已展示。批量请求时，在统一错误处理里先判断标志位，没弹过才提示一次并置为已弹。单个请求失败也走同一套逻辑，避免和批量逻辑重复弹窗。根据产品需求决定一轮请求结束后是否重置标志位。||口述：设一个「是否已提示」的状态锁，所有错误入口先检查它，保证同一轮失败只弹一次。
2||用对象映射或策略模式代替多层 if-else，按类型查表执行。复杂流程用状态机，用枚举和转移表描述。多用提前 return 和卫语句减少嵌套。能配置化的分支放配置表，别硬编码。||口述：优先映射表和策略模式，流程复杂就上状态机，减少嵌套。
3||babel-runtime 是 Babel 转译需要的运行时辅助包，避免每个文件重复生成相同 helper。提供 Promise、Symbol 等 polyfill 和 async 支持。配合 transform-runtime 插件，把内联 helper 改成从 runtime 引用，减小体积。||口述：babel-runtime 提供转译后的公共运行时，避免重复打包，配合 transform-runtime 使用。
4||最简单是新窗口或 a 标签直接打开 PDF。要内嵌预览可用 pdf.js 渲染到 canvas，自定义翻页和工具栏。也可用 iframe 嵌入。选型看是否要下载、批注、跨域和移动端支持。||口述：简单场景新窗口打开；要页面内预览用 pdf.js 画到 canvas。
5||监听右键事件并阻止浏览器默认菜单。根据鼠标位置或当前选区弹出自定义菜单。菜单项绑定复制、标注等操作。点击其他区域关闭菜单，注意移动端长按兼容。||口述：拦截默认右键菜单，根据选区弹自定义菜单做业务操作。
6||监听鼠标抬起或选区变化，用 Selection API 获取选中文本范围。判断选区是否在富文本容器内。对选区打标或触发操作菜单。协同场景还要考虑冲突和撤销。||口述：用 Selection 拿选区，确认在编辑器内后做高亮或菜单。
7||采集分三类：错误、性能、行为。上报要采样、批量、可离线。配合告警和 SourceMap 还原堆栈。先定 SLI，再决定采什么、怎么告警。||口述：从错误、性能、行为建监控，采样上报加告警闭环。
8||统一反馈入口和字段：环境、步骤、截图、账号。自动带版本、UA、链路 ID。按优先级分级响应。和日志、监控系统打通，处理完要回访闭环。||口述：标准化反馈表单，自动带环境信息，按优先级流转并闭环。
9||动态设置根元素字号，比如 1rem 对应设计稿十分之一宽度。样式尺寸按比例写成 rem。可用构建工具自动把 px 转 rem。||口述：设好 html 根字号，元素尺寸用 rem 按比例适配。
10||同源策略限制的是脚本跨域读取响应，不是禁止加载资源。script、link、img 标签可跨域加载，只是页面 JS 读不到对方内容。CDN 静态资源不需要读响应体，所以不受限。||口述：限制的是 JS 读跨域响应，CDN 用标签加载资源没问题。
11||默认不能跨域共享。同父域可设 domain 共享子域。跨站要看 SameSite 和 Secure 政策，第三方 Cookie 越来越严。复杂场景用 Token 或 SSO。||口述：默认同域；子域设 domain；跨站靠 Token 更靠谱。
12||可以取消。发请求时绑定取消控制器，组件卸载或新请求前触发取消。要在错误处理里区分「主动取消」和「真失败」。||口述：请求绑取消令牌，离开页面或重复搜索时取消旧请求。
13||简单场景用原生折叠标签。要动画和手风琴互斥，用 JS 切换展开状态和样式。注意无障碍属性。||口述：简单用原生折叠，要动效和互斥用 JS 控制 class。
14||用 contains 判断包含关系，或沿 parentNode 向上找是否经过目标节点。注意 Shadow DOM 边界。||口述：直接用 contains，或往上遍历父节点判断。
15||先明确「空」是否包含原型链。只判自身可用键名判断；含原型链要沿原型查自有属性。面试时先对齐定义。||口述：先说明判的是自身还是含原型链，再选对应判断方式。
16||先统一业务定义：0 和空字符串算不算空。null、undefined 单独判。数组看长度，对象看键，Map 和 Set 看 size。封装统一判空工具按类型分支。||口述：按类型分别判断，封装统一 isEmpty，提前和产品对齐边界。
17||容器正反面两张，用 3D 旋转实现翻转，背面默认隐藏。hover 或点击时旋转父容器。||口述：正反面叠在一起，用 3D 翻转和背面隐藏做翻牌。
18||表示在弹性布局里占满剩余空间，可伸可缩，基础尺寸为 0。和 auto、none 含义不同。||口述：flex:1 就是参与分配剩余空间，三个子属性组合的简写。
19||小步重构，每次可验证。先补测试再改。提取重复逻辑，大改用分支灰度。||口述：小步迭代、先补测试、提取重复，大改动灰度验证。
20||用静态分析找未使用代码和依赖。构建时 Tree Shaking 去掉死代码。CSS 按实际使用裁剪。定期看打包分析报告。||口述：静态分析找死代码，构建摇树，CSS 按使用裁剪。
21||文案抽离到语言包。用国际化框架做切换、插值和复数。日期货币按地区格式化。||口述：文案外置语言包，用 i18n 框架管理和切换。
22||按用户标签分流到新版本。配合功能开关控制特性。监控指标逐步放量，异常快速回滚。||口述：按标签切流量，配合开关，看指标逐步放量。
23||iframe 隔离强但体验差：双滚动条、通信麻烦、加载慢、SEO 差。适合强隔离弱交互场景。现代微前端多用 JS 沙箱。||口述：iframe 隔离好但体验和通信差，一般微前端不用它做主方案。
24||为每个子应用创建沙箱全局对象，全局变量写进沙箱而非真 window。卸载时销毁沙箱。配合样式隔离防污染。||口述：用沙箱隔离子应用全局变量，卸载时一起销毁。
25||JS 用沙箱隔离全局，CSS 用前缀或 Shadow DOM，应用间用约定事件或总线通信，主应用调度路由和资源。||口述：分 JS、CSS、通信三层做隔离。
26||用下标当 key，列表重排时会错复用节点，导致状态和性能问题。应用业务唯一标识。||口述：key 必须稳定唯一，别用 index。
27||按更新频率拆分 context，稳定 value 引用，状态下沉到子树，避免所有消费者因无关更新重渲染。||口述：拆 context、稳定引用、状态下沉，减少连带刷新。
28||把目标区域绘制到 canvas 再导出图片，或用现成截图库把 DOM 转成图。注意跨域资源和内存。||口述：DOM 转 canvas 再导出，注意跨域图和大图内存。
29||前端：CDN、缓存、降级、限流提示。后端：扩容、负载均衡、缓存、异步削峰。全链路监控和熔断。||口述：前后端协同，缓存削峰扩容，非核心功能可降级。
30||超大整数用 BigInt，高精度小数用专用 decimal 库。金额别用裸浮点。||口述：大整数 BigInt，高精度用 decimal 库。
31||服务端根据设备类型返回不同页面或重定向。也可用响应式一套代码。推荐服务端识别保证首屏正确。||口述：服务端看设备类型返回 PC 或 H5，或响应式一套适配。
32||从性能、交互反馈、稳定性、用户反馈四条线保证体验，不只看技术指标。||口述：快、稳、有反馈、能闭环，才算好体验。
33||接口合并、BFF 聚合、缓存分页。静态资源 CDN、懒加载、代码分割。浏览器缓存和 HTTP/2。||口述：接口侧合并缓存，资源侧 CDN 和按需加载。
34||统一封装请求记录耗时，上报服务端做 P50/P95 分析。可用 Performance API 或拦截器。注意采样和隐私。||口述：请求层打点上报，大盘看慢接口和 P95。
35||文件切片上传，支持断点续传和秒传，控制并发并展示进度。||口述：切片、断点、秒传、控并发，四件事。
36||viewport 加 rem 或 vw，处理 1px 边框和安全区，横竖屏适配。||口述：视口、相对单位、安全区三件事。
37||用 CSS 变量存主题色，切换根节点 data-theme 即可。可记住用户选择。||口述：CSS 变量 + 切换根属性实现换肤。
38||路由和请求拦截里控制进度条。多请求用引用计数，全部完成再关闭。||口述：全局拦截显示进度，多请求计数防提前关。
39||图片进入视口才加载，可用原生 lazy 或视口监听。列表大数据用虚拟滚动。||口述：懒加载图片，长列表只渲染可见区。
40||包含键值、作用域、过期时间，以及 Secure、HttpOnly、SameSite 安全属性。||口述：名值对加作用域、过期和安全三个属性。
41||PC 展示二维码，手机扫码确认，PC 轮询或长连接拿登录结果。注意过期和防伪。||口述：二维码做信令，手机授权，PC 等结果。
42||域名解析成 IP，先查缓存再递归查询。理解 TTL 和记录类型即可。||口述：先缓存后递归，把域名翻译成 IP。
43||函数是一等公民，强调纯函数、不可变、组合式处理数据。||口述：无副作用、不可变、用组合代替命令式循环。
44||页面铺半透明水印，监听 DOM 被改就恢复，提高删除成本，无法绝对防。||口述：水印 + 防删监听，只能提高泄露成本。
45||把业务概念抽象成对象，统一前后端语言，减少沟通偏差。||口述：领域模型是业务概念的代码抽象。
46||易命名冲突、难维护、有安全风险、可能造成内存泄漏。应用模块化管理。||口述：别乱挂全局，污染作用域还有安全风险。
47||语义化标签、SSR 预渲染、完善 meta 和 sitemap，保证爬虫能读到内容和结构。||口述：让爬虫拿到完整 HTML 和元信息，性能也是 SEO。
48||逻辑层跑业务，渲染层跑视图，通过数据下发通信，避免 JS 直接操作 DOM 阻塞。||口述：双线程分工，逻辑和渲染分离保流畅。
49||图片失败用占位，CDN 失败切备用源，接口失败用缓存或友好降级页。||口述：分资源类型准备占位、备用源和缓存兜底。
50||标准自定义属性，给元素存私有数据，通过 dataset 读取。||口述：data- 是放自定义数据的标准方式。
51||下拉判断位移触发刷新，滚到底部加载更多，注意防抖和 loading 态。||口述：touch 下拉刷新，scroll 触底加载。
52||对比元素位置和视口，或用视口交叉监听，适合懒加载场景。||口述：算位置或监听进入视口来判断可见。
53||画布画座位网格，点击算命中哪个座位，颜色区分可选已售。||口述：坐标命中 + 状态颜色，数据驱动重绘。
54||写入时带时间戳，读取时判断过期并删除。localStorage 本身无过期要自己实现。||口述：自管 TTL，过期就清。
55||手动配构建工具入口、加载器、插件，装 React 和转译工具即可跑起来。||口述：和脚手架一样，只是配置手写。
56||递归遍历目录，过滤目标扩展名，逐文件统计行数再汇总。||口述：遍历、过滤、累加三步。
57||告诉打包工具哪些文件有副作用不能删，没引用的才可安全摇树。||口述：标记副作用，帮 Tree Shaking 判断能不能删。
58||src 指定地址，async 尽快执行，defer 等 DOM 解析完，module 表示 ES 模块。||口述：async 和 defer 都不挡解析，执行时机不同。
59||hash 改地址不请求服务器，静态部署友好，但 SEO 不如 history 模式。||口述：纯静态托管时常用 hash 避免刷新 404。
60||用路由库的 hooks 或监听 history 变化感知跳转。||口述：封装路由变化监听，驱动页面切换。
61||统一认证中心登录一次，各系统拿 ticket 换本地会话。||口述：中心认证 + ticket 换 session。
62||水印多层铺设并监听 DOM 变化，被删就恢复，提高篡改成本。||口述：提高删水印成本，无法绝对防止。
63||先看控制台报错和网络失败，结合最近发版和 SourceMap 定位。||口述：Console + Network + 发版记录三板斧。
64||按类型比较，对象递归比属性，注意循环引用和日期等特殊类型。||口述：分类型比，递归对象，处理环和特殊值。
65||UI 是状态的映射，改数据自动更新视图，靠响应式或 setState。||口述：状态驱动视图，框架负责自动更新。
66||官方脚手架，集成创建、构建、热更新、lint 和插件生态。||口述：帮你配好 Vue 工程化工具链。
67||任务切片，每批执行一点让出主线程，或用后台线程，别一次算百万次。||口述：时间切片，分批算，避免卡死。
68||head 里同步脚本会挡解析，放底部或 defer 更好。||口述：同步脚本挡渲染，defer 或放底部。
69||源码变语法树，按规则遍历检查，可自动修复部分问题。||口述：AST + 规则检查 + 报告。
70||只渲染视口内几项，滚动换数据，用占位撑开总高度。||口述：窗口化渲染，十万条也只几十个 DOM。
71||react-router 提供组件化路由、嵌套路由、路由守卫和 hooks。原生要自己监听 history、匹配路径、渲染组件。||口述：react-router 封装好了，原生得自己管 history 和匹配。
72||块级独占一行可设宽高，行内和其他行内排一行，宽高由内容决定。||口述：块级换行占满宽，行内不换行随内容。
73||浏览器空闲时执行低优先级任务，适合非紧急工作，超时会被打断让出。||口述：主线程闲了再干杂活，别挡用户交互。
74||轻量文档片段，批量改 DOM 时先挂片段再一次性插入，减少重排。||口述：离线拼 DOM，一次插入，少触发重排。
75||fetch 只拉远程更新不合并，pull 拉完还合并到当前分支。||口述：fetch 看远程，pull 还要合并。
76||CSS 变量存主题色，切换根 class 或 data 属性，可记用户偏好。||口述：变量 + 根节点切换实现主题。
77||监控告警、灰度发布、错误边界、降级预案、容量评估，前端也要参与稳定性。||口述：监控、灰度、降级、预案，前端不能只管页面。
78||用 PerformanceObserver 监听长任务，记录持续时间和次数上报。||口述：长任务 API 打点，统计卡顿次数。
79||即时编译，热点代码运行时编译成机器码加速，冷代码仍解释执行。||口述：跑得多的代码现场编译，越跑越快。
80||按分号拆各 cookie，再按等号拆名值，注意解码和重复名处理。||口述：字符串拆分解析，注意 decode 和边界。
81||构建时给组件样式加唯一属性选择器，运行时只影响本组件。||口述：编译加唯一标记，选择器带上这个标记。
82||CSS Modules、BEM、Shadow DOM、scoped、CSS-in-JS 等。||口述：命名约定、作用域、Shadow DOM 几类方案。
83||改迭代为循环，或尾递归优化，深递归用队列模拟，必要时报栈深度限制。||口述：递归改循环，或用队列模拟。
84||限流、验证码、登录墙、接口签名校验、混淆提高成本，无法完全杜绝。||口述：提高爬取成本，核心数据后端校验。
85||在 tsconfig 里配置 types 或 paths，把全局类型映射到项目里用。||口述：改 tsconfig 的 types/paths 引入全局类型。
86||localStorage 事件、BroadcastChannel、SharedWorker、postMessage  opener。||口述：同源用 storage 事件或 BroadcastChannel，跨窗 postMessage。
87||可以，但要自己封装 history 监听、路径匹配和组件渲染，成本高。||口述：能不用库，但要自己造路由轮子。
88||校验失败后取第一个错误表单项，算位置 scrollIntoView 滚过去。||口述：找到第一个报错项，滚到视口里。
89||虚拟列表只渲染可见项，滚动换数据，别一次塞十万 DOM。||口述：虚拟滚动，视口内才渲染。
90||根据模块内容、chunk 关系、运行时代码等算哈希，内容变 hash 变。||口述：对模块内容算摘要，变了 hash 就变。
91||规范、脚手架、CI/CD、组件库、监控、文档、发布流程一套齐。||口述：工程规范 + 工具链 + 组件库 + 监控发布。
92||类型系统、泛型、联合交叉、工具类型、枚举、装饰器等，结合项目说用过的。||口述：按你项目实际用过的 TS 能力举例。
93||同步脚本会阻塞解析，async/defer/module 不阻塞或延迟执行。||口述：同步挡解析，defer/async 改善加载。
94||HTTP/2 多路复用、HTTP/3 QUIC、资源优先级、预连接预加载。||口述：多路复用和协议升级缓解队头阻塞。
95||外部 script 当静态资源，注意全局变量污染，可用 externals 或异步加载。||口述：当第三方静态资源管，防污染和重复加载。
96||轮询版本号文件、WebSocket 推送、Service Worker 更新提示用户刷新。||口述：检测新版本，弹窗或提示让用户主动刷。
97||源码变 AST，按规则树遍历检查，可自动修复。||口述：和前面 ESLint 题一样，AST + 规则。
98||Session、Token、Cookie 会话、刷新令牌，前后端约定存储和续期。||口述：无状态靠 Token 或 Session 标识用户。
99||监听鼠标键盘滚动等用户事件，定时器判断多久无操作算空闲。||口述：记最后操作时间，超时算空闲。
100||开发用原生 ESM 按需编译，生产用 Rollup，冷启动和热更都比传统打包快。||口述：开发不打包按需编译，所以 Vite 快。
101||给请求加序号或取消旧请求，只采纳最新一次响应，避免旧数据覆盖新页。||口述：快速翻页时取消旧请求或比序号，只要最新的。
102||时间切片分批执行，每批让出主线程，显示进度。||口述：和 67 题一样，切片别一次算完。
103||先 mirror 或加新 remote，fetch 新仓库，切分支推送，改默认远程，通知团队。||口述：加新地址、拉代码、推过去、改 remote、通知同事。
104||禁用右键和快捷键只能防小白，devtools 拦不住。真正敏感逻辑放后端。||口述：前端防调试只是增加成本，核心逻辑别放前端。
105||合适格式和尺寸、懒加载、响应式图、CDN、雪碧图、WebP/AVIF、压缩。||口述：格式、尺寸、懒加载、CDN、压缩几件事。
106||授权第三方代用户访问资源的开放标准，常见授权码模式。||口述：OAuth 是授权协议，不是登录协议本身。
107||统一认证中心登录，各系统拿票据换本地会话，一次登录多处通行。||口述：和 61 题类似，中心认证 + 票据换 session。
108||Session-Cookie、JWT、OAuth、SSO、双 Token 等，说清适用场景。||口述：按场景选 Session、JWT 或 SSO。
109||跨域默认不带 Cookie，要服务端配 CORS 允许凭证，前端设 withCredentials，Cookie 设 SameSite=None; Secure。||口述：前后端都要开凭证，Cookie 属性也要配对。
110||Vite 利用 ESM 精确热更，Webpack 大项目往往要重新构建依赖链。||口述：Vite 热更更精准，Webpack 大项目慢一些。
111||设超时定时器，超时 abort 重试，限制最大重试次数和退避间隔。||口述：超时取消，指数退避重试，设上限。
112||封装请求时设 timeout，或用 AbortController 配合定时器。||口述：请求层配超时，到期主动取消。
113||主进程 cluster 或 worker_threads 多进程/多线程吃满多核。||口述：Cluster 多进程或 Worker 线程分摊计算。
114||虚拟滚动、懒加载子节点、分页展开、Web Worker 处理，别一次渲染整棵树。||口述：树也要虚拟化和按需展开。
115||单一职责、接口稳定、可组合、文档齐全、少暴露实现细节。||口述：小而稳、接口清晰、可复用可测试。
116||从用户感知、网络、渲染、资源体积、缓存、接口多条线想，别只盯某一个指标。||口述：性能优化要多维度，别只压缩图片。
117||统一采集 API、自动带环境信息、采样批量上报、错误去重、用户隐私合规。||口述：统一入口、自动上下文、采样上报。
118||登录后发 access token，请求带 Authorization，可配 refresh token 续期，注意存储安全。||口述：Token 标识身份，短效访问长效刷新，安全存储。
119||路由级、菜单级、按钮级权限，前后端都要校验，前端做体验后端做安全。||口述：RBAC 或 ABAC，前后端双重校验。
120||Schema 描述组件树，渲染器递归遍历 Schema 生成真实组件。||口述：用 JSON Schema 驱动，渲染器递归出 UI。
121||定义组件、属性、事件、布局的协议格式，保证编辑器与运行时一致。||口述：底层协议统一描述 UI 和交互。
122||代码分割、缓存、压缩、Tree Shaking、CDN、图片优化、分析打包体积。||口述：拆包、缓存、压缩、摇树、CDN 一套组合拳。
123||浏览器按源配额限制，用户可授权更多，不同浏览器策略略有差异。||口述：按源分配空间，可申请更大配额。
124||Cookie、localStorage、sessionStorage、IndexedDB、Cache API 等，说清容量和场景。||口述：短期 Cookie，大量结构化用 IndexedDB。
125||把运行时代码单独抽 chunk，HTML 里先加载 runtime 再加载业务包。||口述：runtime 单独打，页面先引它再引业务。
126||大数据量表格 canvas 性能更好，SVG DOM 节点太多会卡，canvas 绘制更轻。||口述：海量单元格 canvas 更快，SVG 适合量小可缩放矢量场景。
127||结合项目说：HtmlWebpackPlugin 生成 HTML、MiniCssExtract 抽 CSS、DefinePlugin 注入环境变量等。||口述：按你项目用过的 plugin 说作用和场景。
128||结合项目说：babel-loader 转 JS、css-loader 处理样式、file-loader/url-loader 处理资源等。||口述：loader 是文件级转换，按类型选用。
129||memo、useMemo、useCallback、拆分组件、状态下沉、避免匿名函数和对象字面量当 props。||口述：减少不必要渲染靠 memo 和稳定 props。
130||CSS Modules、BEM、CSS-in-JS、提高选择器优先级、Shadow DOM 隔离。||口述：命名隔离或作用域隔离，别全局污染。
131||CSSTransition/TransitionGroup 或动画库，配合路由切换加 enter/leave 类名。||口述：过渡类名或动画库控制进出场。
132||memo、懒加载、虚拟列表、状态下沉、避免 context 大范围更新、列表 key 正确。||口述：和 129 类似，从渲染、列表、状态几方面说。
133||v-for 优先级高于 v-if，同元素会先循环再判断，浪费性能且逻辑易错。应 template 包一层或 computed 过滤。||口述：同元素会先循环再判断，应分开写或先过滤数据。
134||强缓存、协商缓存、Service Worker、localStorage 存资源、离线包。||口述：HTTP 缓存 + SW + 本地存储几层。
135||路由懒加载、拆 vendor、SSR/预渲染、CDN、压缩、预加载关键资源、骨架屏。||口述：拆包、CDN、SSR、预加载，减少首屏体积。
136||axios 根据运行环境自动选 xhr 或 http 适配器，也可用环境变量区分。||口述：浏览器走 xhr，Node 走 http 模块。
137||Service Worker 拦截 fetch、代理层、Mock 服务、浏览器扩展等。||口述：SW 或代理层拦截请求做缓存或 Mock。
138||postMessage、BroadcastChannel、localStorage 事件、SharedWorker、URL 参数、服务端中转。||口述：同源用 Channel 或 storage 事件，跨源 postMessage 或服务端。
'''

answers = {}
for line in RAW.strip().splitlines():
    if not line.strip():
        continue
    num_s, body = line.split('||', 1)
    answers[int(num_s)] = [p.strip() for p in body.split('||') if p.strip()]

content = '# -*- coding: utf-8 -*-\n"""138 道场景题口述版答案（面试人话，无代码）"""\n\nSPOKEN_ANSWERS = {\n'
for k in sorted(answers):
    content += f'    {k}: [\n'
    for p in answers[k]:
        content += f'        "{p}",\n'
    content += '    ],\n'
content += '}\n'

OUT.write_text(content, encoding='utf-8')
print(f'Wrote {OUT} with {len(answers)} answers')
